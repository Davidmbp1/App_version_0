rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir acceso público SOLO a la carpeta videos_onb_instr
    match /videos_onb_instr/{file} {
      allow read: if true;
    }
    // MEJOR SOLUCIÓN: Renombrar la carpeta a "Revision_de_callos" (sin espacios)
    // Luego usar esta regla:
    match /Revision_de_callos/{file} {
      allow read: if true;
    }
    // Requiere autenticación para el resto del bucket
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
